services:
  app:
    build: ./
    restart: always
    ports:
      - 8000:8000
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: 12345
    ports:
      - 5436:5432